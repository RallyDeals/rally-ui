export interface OrderSummary {
  subtotal: string;
  shippingLabel: string;
  shippingCost: string;
  estimatedTaxes: string;
  total: string;
  totalNote: string;
  estimatedDeliveryRange: string;
}
